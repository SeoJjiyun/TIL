너무 if else 가 많아지면 상속의 여부를 생각해보기 

비슷한 것은 있지만 기능이 조금 다를 경우 또다른 클래스를 만들어보자 
-> 이미 구현된 클래스와 내용이 중복될 경우 클래스를 확장하여서 구현(상속)

이때 private으로 한 값이 있다면, 
상속에서 사용불가능하므로, protected로 사용한다면
외부클래스에서는 사용 불가능 하지만, 상위클래스에서는 하위클래스의 변수를 사용가능하다
