final 
1) 변수에 쓰이면 : 상수 
2) 클래스에 사용하는 경우 상속이 안됨
3) 메서드로 쓰이면 재정의 안되는 메서드

공통으로 사용하는 상수 값이나 변수의 경우, Define 함수에 넣어서 한번에 불러오자 !

인터페이스 왜 사용이유
인터페이스 : 어떤 연결점 - 예를 들어 서버와 클라이언트 사이의 연결점(클라이언트는 인터페이스를 보고 구현된 클래스를 사용)

버전관리가 중요함
하나의 소스로 인터페이스를 구성해서 그것을 기반으로 implement를 통해서 oracle 또는 mysql등등으로 구성하여 오버헤드가 적게 실행되도록 한다.

[DB]
ORACLE, MYSQL 등등이 존재, 데이터들을 관리할 수 있는 역할

[DB&Interface]
인터페이스 없이 그대로 DB를 만들면 다른 언어로 만들 경우 중복이되고, 오버헤드가 커질 수 있음.
따라서 인터페이스를 만들어 각각의 타입을 만들어놓음 만약 파일이 ORACLE일경우 ORACLEDao를 MySQL이면 MySQLDao를 사용

[DB불러오기]
1.
